using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "SOTeams",menuName = "SOS/SOTeams")]
public class SOPlayers : ScriptableObject
{
    // Start is called before the first frame update
    public int p1Team;
    public int p2Team;
}
